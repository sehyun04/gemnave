# 젬나브

로스트아크 젬 가공 화면을 읽어 **지금 뜬 4개로 굴릴지, 리롤할지** 답하는 계산기.
의존성도 빌드 단계도 없는 정적 페이지다.

## 스타일

- **이모지 금지.** 새로 작성/수정하는 코드에는 이모지를 쓰지 않는다.
  아이콘이 필요하면 유니코드 이모지(📍🃏 등) 대신 SVG 로 그리거나, 텍스트와
  이미 쓰던 기호(`·`, `└`, `-`)를 쓴다.
- **주석은 why 만.** 코드가 이미 말하는 what 은 반복하지 않고, 그렇게 짠 이유·배경·제약만 남긴다.
- 문서와 주석은 한국어.

## 작업 전에 읽을 것

순서대로:

1. [`README.md`](README.md) - 계산 방식, 구조, UI 설계 이유, 남은 일
2. [`vision/README.md`](vision/README.md) - 화면 인식 전부.
   **이미 측정해서 버린 방법들이 적혀 있다.** 여기 "틀렸다"고 적힌 걸 다시 시도하지 말 것

## 검증

바꾼 뒤에는 반드시 둘 다 통과해야 한다.

```
node test.js          # 확률 표 + 솔버 (26)
node vision/test.js   # 화면 인식 (109) - 캡처 49장을 다 읽어서 1분쯤 걸린다
```

빌드 단계가 없다. 로컬 서버로 띄워서 눈으로 본다.

```
python -m http.server 8080
```

`fetch` 와 Web Worker 를 쓰므로 `file://` 로 열면 안 된다.

레이아웃은 `playwright` 로 폭을 바꿔가며 훑는다(1600 / 1366 / 1100 / 760 / 390, 그리고
경고가 다 뜬 상태). 사람 눈으로는 안 잡히는 것이 나온다. 자세한 것은 README 의 "검수" 절.

## 어기면 안 되는 것

### 확률 표를 임의로 고치지 않는다

`rules.js` 는 스마일게이트 공식 확률 공개 페이지 값이고 27개 합이 정확히 100.0000% 다.
추정치가 아니다. 숫자 하나를 바꾸면 결과 전체가 거짓이 된다. 근거 없이 손대지 말 것.

### 조용히 틀리지 않는다

이 프로젝트의 핵심 불변식이다. 화면 인식은 **모르면 모른다고 해야 한다.**

- 확신이 없는 값은 입력칸에 넣지 않고 의심 표시로 남긴다. 넣고 나서 사용자가
  알아서 검증하기를 기대하지 않는다.
- 젬 포인트(= 네 수치의 합)로 검산한다. 한 자리가 애매하면 합으로 복구하고,
  합이 안 맞으면 네 자리 전부 의심으로 내린다(`reader.reconcileGem`).
- 접이식 패널은 접혀 있어도 **무엇이 들어갔는지 한 줄 요약을 보여준다.**
  안 보이면 잘못 읽은 값으로 계산된 답을 그대로 믿게 된다.
- 읽기 전에는 답을 내지 않는다(`gemKnown`). 기본값으로 계산해서 확률을 띄우면
  아무것도 안 읽었는데 읽은 것처럼 보인다. 한 번 그렇게 만들었다가 고쳤다.
- 화면에는 보이는데 지금 수치에서 나올 수 없는 항목이면 알린다.

점수 문턱을 낮춰서 "인식률"을 올리는 변경은 이 불변식을 깨는 것이다. 하지 말 것.

### 문구의 주인은 index.html 이다

사용자에게 보이는 문구는 `index.html` 에 있고 `app.js` 가 그걸 읽어다 쓴다
(`IDLE_LIVE = $('liveState').textContent`). 같은 문장을 두 파일에 두면 한쪽만 고쳐진다.

- **`app.js` 에 안내 문구를 하드코딩하지 말 것.**
- 저장소 주인이 `index.html` 을 직접 고친다. 문구를 "고쳐 주지" 말 것.
- HTML 에서 요소가 빠져도 `app.js` 가 터지면 안 된다. 실제로 `pickSummary` span 이
  빠졌을 때 `null.textContent` 로 계산 전체가 죽은 적이 있다. 널 가드를 둔다.

### app.js 는 확률을 계산하지 않는다

전부 워커 안의 `solver.js` 가 한다. UI 쪽에서 확률을 흉내내면 `test.js` 가
검증하지 못하는 두 번째 진실이 생긴다.

### 색은 정보다

강조색이 곧 젬 계열이다(질서 빨강 / 혼돈 파랑). 그래서 읽기 전에는 색을 주지 않는다.
장식으로 색을 쓰면 이 신호가 죽는다. 기본 강조색을 흔한 AI 보라 같은 것으로
되돌리지 말 것.

## 디자인 스킬

`impeccable`, `emilkowalski/skills`, `taste-skill` 이 깔려 있다(`skills-lock.json`).
본체는 추적하지 않으므로 없으면 재설치한다.

```
npx impeccable install
npx skills@latest add emilkowalski/skills
npx skills add https://github.com/Leonxlnx/taste-skill
```

`impeccable` 이 `.claude/settings.local.json` 에 훅을 심는다. UI 파일을 고친 뒤와 턴이
끝날 때 검사가 돌고 결과가 대화에 들어온다. 훅 경로(`hook.mjs`, `hook-lib.mjs`)에
네트워크 호출은 없다.

검출기를 직접 돌리려면 `node .claude/skills/impeccable/scripts/detect.mjs <파일...>`.
**알려진 오탐:** `background: <gradient> var(--bg)` 단축의 마지막 레이어를 못 풀어서
배경을 흰색으로 가정하고 low-contrast 를 잘못 띄운다. 실제 색은 열네 쌍 전부 WCAG AA
를 넘는다(가장 낮은 것이 `--dim` 5.48:1).

## 이력

원래 [2rami/loa-bot](https://github.com/2rami/loa-bot) 의 `web/gempago/` 에 있었고,
`git subtree split` 으로 커밋 32개를 그대로 들고 나왔다. 그래서 초기 커밋 메시지에
`web/gempago/` 경로가 남아 있다. `gempago-poc` 는 Tesseract 로 만든 첫 버전을
가리키는 옛 디렉터리 이름이다(`vision/README.md` 참고).
